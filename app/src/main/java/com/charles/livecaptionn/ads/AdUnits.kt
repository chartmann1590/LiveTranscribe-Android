package com.charles.livecaptionn.ads

/**
 * AdMob unit IDs for LiveCaptionN. These are public identifiers (the same
 * values the Play Store build ships to every device) so they are fine to
 * commit to source control.
 */
object AdUnits {
    /** App-open ad shown when the user brings the app back to foreground. */
    const val APP_OPEN = "ca-app-pub-8382831211800454/6447868787"

    /** Banner pinned to the bottom of the main UI (not the overlay). */
    const val BANNER = "ca-app-pub-8382831211800454/2231636617"
}
