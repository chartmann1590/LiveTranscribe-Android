New: the entire app interface can now be translated into 59 languages.

• Pick a language in Settings or on first launch — menus, settings, overlay controls, and notifications all switch to it
• Models download once (~30 MB), then the whole UI works fully offline
• Shows download/translate progress and falls back automatically if a model can't be fetched
